# Unix
alias ll="ls -al"
alias ln="ln -v"
alias mkdir="mkdir -p"
alias e="$EDITOR"
alias v="$VISUAL"

alias gs="git status"
alias ga="git add"
alias gc="git commit"
alias gco="git checkout"
alias h="history"
alias v="mvim"

# Bundler
alias b="bundle"

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

alias python='python3'
alias pip='pip3'
